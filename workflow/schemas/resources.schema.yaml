$schema: "http://json-schema.org/draft-04/schema#"
description: snakemake configuration file
type: object
properties:
  default_resources:
    type: object
    properties:
      threads:
        type: integer
        description: default number of threads that will be used by a rule
      time:
        type: string
        description: default max execution time for a rule
      mem_mb:
        type: integer
        description: default memory used for a rule
      mem_per_cpu:
        type: integer
        description: default memory used per cpu for a rule
      partition:
        type: string
        description: default partition to use on the cluser for a rule
    required:
      - threads
      - time
      - mem_mb
      - mem_per_cpu
      - partition

  cnvkit_batch:
    description: resource definitions for cnvkit_batch
    type: object
    properties:
      threads:
        type: integer
        description: default number of threads that will be used by 'cnvkit_batch'
      time:
        type: string
        description: default max execution time for 'cnvkit_batch'
      mem_mb:
        type: integer
        description: memory used for 'cnvkit_batch'
      mem_per_cpu:
        type: integer
        description: memory used per cpu for 'cnvkit_batch'
      partition:
        type: string
        description: partition to use on the cluster for 'cnvkit_batch'

  cnvkit_call:
    description: resource definitions for cnvkit_call
    type: object
    properties:
      threads:
        type: integer
        description: default number of threads that will be used by 'cnvkit_call'
      time:
        type: string
        description: default max execution time for 'cnvkit_call'
      mem_mb:
        type: integer
        description: memory used for 'cnvkit_call'
      mem_per_cpu:
        type: integer
        description: memory used per cpu for 'cnvkit_call'
      partition:
        type: string
        description: partition to use on the cluster for 'cnvkit_call'

  cnvkit_diagram:
    description: resource definitions for cnvkit_diagram
    type: object
    properties:
      threads:
        type: integer
        description: default number of threads that will be used by 'cnvkit_diagram'
      time:
        type: string
        description: default max execution time for 'cnvkit_diagram'
      mem_mb:
        type: integer
        description: memory used for 'cnvkit_diagram'
      mem_per_cpu:
        type: integer
        description: memory used per cpu for 'cnvkit_diagram'
      partition:
        type: string
        description: partition to use on the cluster for 'cnvkit_diagram'

  cnvkit_scatter:
    description: resource definitions for cnvkit_scatter
    type: object
    properties:
      threads:
        type: integer
        description: default number of threads that will be used by 'cnvkit_scatter'
      time:
        type: string
        description: default max execution time for 'cnvkit_scatter'
      mem_mb:
        type: integer
        description: memory used for 'cnvkit_scatter'
      mem_per_cpu:
        type: integer
        description: memory used per cpu for 'cnvkit_scatter'
      partition:
        type: string
        description: partition to use on the cluster for 'cnvkit_scatter'

  cnvkit_vcf:
    description: resource definitions for cnvkit_vcf
    type: object
    properties:
      threads:
        type: integer
        description: default number of threads that will be used by 'cnvkit_vcf'
      time:
        type: string
        description: default max execution time for 'cnvkit_vcf'
      mem_mb:
        type: integer
        description: memory used for 'cnvkit_vcf'
      mem_per_cpu:
        type: integer
        description: memory used per cpu for 'cnvkit_vcf'
      partition:
        type: string
        description: partition to use on the cluster for 'cnvkit_vcf'

  cnvkit_export_seg:
    description: resource definitions for cnvkit_export_seg
    type: object
    properties:
      mem_mb:
        type: integer
        description: max memory in MB to be available
      mem_per_cpu:
        type: integer
        description: memory in MB used per cpu
      partition:
        type: string
        description: partition to use on cluster
      threads:
        type: integer
        description: number of threads to be available
      time:
        type: string
        description: max execution time

  cnvpytor:
    description: resource definitions for cnvpytor
    type: object
    properties:
      mem_mb:
        type: integer
        description: max memory in MB to be available
      mem_per_cpu:
        type: integer
        description: memory in MB used per cpu
      partition:
        type: string
        description: partition to use on cluster
      threads:
        type: integer
        description: number of threads to be available
      time:
        type: string
        description: max execution time

  expansionhunter:
    description: resource definitions expansionhunter
    type: object
    properties:
      mem_mb:
        type: integer
        description: memory in MB used per cpu
      mem_per_cpu:
        type: integer
        description: memory used per cpu
      partition:
        type: string
        description: partition to use on cluster
      threads:
        type: integer
        description: number of threads to be available
      time:
        type: string
        description: max execution time

  gatk_denoise_read_counts:
    description: resource definitions for gatk_denoise_read_counts
    type: object
    properties:
      mem_mb:
        type: integer
        description: max memory in MB to be available
      mem_per_cpu:
        type: integer
        description: memory in MB used per cpu
      partition:
        type: string
        description: partition to use on cluster
      threads:
        type: integer
        description: number of threads to be available
      time:
        type: string
        description: max execution time

  gatk_collect_read_counts:
    description: resource definitions for gatk_collect_read_counts
    type: object
    properties:
      mem_mb:
        type: integer
        description: max memory in MB to be available
      mem_per_cpu:
        type: integer
        description: memory in MB used per cpu
      partition:
        type: string
        description: partition to use on cluster
      threads:
        type: integer
        description: number of threads to be available
      time:
        type: string
        description: max execution time

  gatk_denoise_read_counts:
    description: resource definitions for gatk_denoise_read_counts
    type: object
    properties:
      mem_mb:
        type: integer
        description: max memory in MB to be available
      mem_per_cpu:
        type: integer
        description: memory in MB used per cpu
      partition:
        type: string
        description: partition to use on cluster
      threads:
        type: integer
        description: number of threads to be available
      time:
        type: string
        description: max execution time

  gatk_model_segments:
    description: resource definitions for gatk_model_segments
    type: object
    properties:
      mem_mb:
        type: integer
        description: max memory in MB to be available
      mem_per_cpu:
        type: integer
        description: memory in MB used per cpu
      partition:
        type: string
        description: partition to use on cluster
      threads:
        type: integer
        description: number of threads to be available
      time:
        type: string
        description: max execution time

  gatk_call_copy_ratio_segments:
    description: resource definitions for gatk_call_copy_ratio_segments
    type: object
    properties:
      mem_mb:
        type: integer
        description: max memory in MB to be available
      mem_per_cpu:
        type: integer
        description: memory in MB used per cpu
      partition:
        type: string
        description: partition to use on cluster
      threads:
        type: integer
        description: number of threads to be available
      time:
        type: string
        description: max execution time

  gatk_cnv_vcf:
    description: resource definitions for gatk_cnv_vcf
    type: object
    properties:
      mem_mb:
        type: integer
        description: max memory in MB to be available
      mem_per_cpu:
        type: integer
        description: memory in MB used per cpu
      partition:
        type: string
        description: partition to use on cluster
      threads:
        type: integer
        description: number of threads to be available
      time:
        type: string
        description: max execution time

  generate_pindel_config:
    description: resource definitions for generate_pindel_config
    type: object
    properties:
      mem_mb:
        type: integer
        description: max memory in MB to be available
      mem_per_cpu:
        type: integer
        description: memory in MB used per cpu
      partition:
        type: string
        description: partition to use on cluster
      threads:
        type: integer
        description: number of threads to be available
      time:
        type: string
        description: max execution time

  manta_config_t:
    description: resource definitions for config_manta_t
    type: object
    properties:
      mem_mb:
        type: integer
        description: max memory in MB to be available
      mem_per_cpu:
        type: integer
        description: memory in MB used per cpu
      partition:
        type: string
        description: partition to use on cluster
      threads:
        type: integer
        description: number of threads to be available
      time:
        type: string
        description: max execution time

  manta_config_tn:
    description: resource definitions for config_manta_tn
    type: object
    properties:
      mem_mb:
        type: integer
        description: max memory in MB to be available
      mem_per_cpu:
        type: integer
        description: memory in MB used per cpu
      partition:
        type: string
        description: partition to use on cluster
      threads:
        type: integer
        description: number of threads to be available
      time:
        type: string
        description: max execution time

  manta_config_n:
    description: resource definitions for config_manta_n
    type: object
    properties:
      mem_mb:
        type: integer
        description: max memory in MB to be available
      mem_per_cpu:
        type: integer
        description: memory in MB used per cpu
      partition:
        type: string
        description: partition to use on cluster
      threads:
        type: integer
        description: number of threads to be available
      time:
        type: string
        description: max execution time

  manta_run_workflow_t:
    description: resource definitions for manta_run_workflow_t
    type: object
    properties:
      mem_mb:
        type: integer
        description: max memory in MB to be available
      mem_per_cpu:
        type: integer
        description: memory in MB used per cpu
      partition:
        type: string
        description: partition to use on cluster
      threads:
        type: integer
        description: number of threads to be available
      time:
        type: string
        description: max execution time

  manta_run_workflow_tn:
    description: resource definitions for manta_run_workflow_tn
    type: object
    properties:
      mem_mb:
        type: integer
        description: max memory in MB to be available
      mem_per_cpu:
        type: integer
        description: memory in MB used per cpu
      partition:
        type: string
        description: partition to use on cluster
      threads:
        type: integer
        description: number of threads to be available
      time:
        type: string
        description: max execution time

  manta_run_workflow_n:
    description: resource definitions for manta_run_workflow_n
    type: object
    properties:
      mem_mb:
        type: integer
        description: max memory in MB to be available
      mem_per_cpu:
        type: integer
        description: memory in MB used per cpu
      partition:
        type: string
        description: partition to use on cluster
      threads:
        type: integer
        description: number of threads to be available
      time:
        type: string
        description: max execution time

  pindel_update_vcf_sequence_dictionary:
    description: resource definitions for pindel_update_vcf_sequence_dictionary
    type: object
    properties:
      mem_mb:
        type: integer
        description: max memory in MB to be available
      mem_per_cpu:
        type: integer
        description: memory in MB used per cpu
      partition:
        type: string
        description: partition to use on cluster
      threads:
        type: integer
        description: number of threads to be available
      time:
        type: string
        description: max execution time

  pindel_call:
    description: resource definitions for pindel_call
    type: object
    properties:
      mem_mb:
        type: integer
        description: max memory in MB to be available
      mem_per_cpu:
        type: integer
        description: memory in MB used per cpu
      partition:
        type: string
        description: partition to use on cluster
      threads:
        type: integer
        description: number of threads to be available
      time:
        type: string
        description: max execution time

  pindel2vcf:
    description: resource definitions for pindel2vcf
    type: object
    properties:
      mem_mb:
        type: integer
        description: max memory in MB to be available
      mem_per_cpu:
        type: integer
        description: memory in MB used per cpu
      partition:
        type: string
        description: partition to use on cluster
      threads:
        type: integer
        description: number of threads to be available
      time:
        type: string
        description: max execution time

  purecn:
    description: resource definitions for purecn
    type: object
    properties:
      mem_mb:
        type: integer
        description: max memory in MB to be available
      mem_per_cpu:
        type: integer
        description: memory in MB used per cpu
      partition:
        type: string
        description: partition to use on cluster
      threads:
        type: integer
        description: number of threads to be available
      time:
        type: string
        description: max execution time

  purecn_copy_output:
    description: resource definitions for purecn_copy_output
    type: object
    properties:
      mem_mb:
        type: integer
        description: max memory in MB to be available
      mem_per_cpu:
        type: integer
        description: memory in MB used per cpu
      partition:
        type: string
        description: partition to use on cluster
      threads:
        type: integer
        description: number of threads to be available
      time:
        type: string
        description: max execution time

  purecn_coverage:
    description: resource definitions for purecn_coverage
    type: object
    properties:
      mem_mb:
        type: integer
        description: max memory in MB to be available
      mem_per_cpu:
        type: integer
        description: memory in MB used per cpu
      partition:
        type: string
        description: partition to use on cluster
      threads:
        type: integer
        description: number of threads to be available
      time:
        type: string
        description: max execution time

  purecn_purity_file:
    description: resource definitions for purecn_purity_file
    type: object
    properties:
      mem_mb:
        type: integer
        description: max memory in MB to be available
      mem_per_cpu:
        type: integer
        description: memory in MB used per cpu
      partition:
        type: string
        description: partition to use on cluster
      threads:
        type: integer
        description: number of threads to be available
      time:
        type: string
        description: max execution time

  reviewer:
    description: resource definitions reviewer
    type: object
    properties:
      mem_mb:
        type: integer
        description: max memory in MB to be available
      mem_per_cpu:
        type: integer
        description: memory in MB used per cpu
      partition:
        type: string
        description: partition to use on cluster
      threads:
        type: integer
        description: number of threads to be available
      time:
        type: string
        description: max execution time

  reviewer_generate_locus_list:
    description: resource definitions for reviewer_generate_locus_list
    type: object
    properties:
      mem_mb:
        type: integer
        description: max memory in MB to be available
      mem_per_cpu:
        type: integer
        description: memory in MB used per cpu
      partition:
        type: string
        description: partition to use on cluster
      threads:
        type: integer
        description: number of threads to be available
      time:
        type: string
        description: max execution time
  
  smn_caller:
    description: resource definitions for smn_caller
    type: object
    properties:
      mem_mb:
        type: integer
        description: max memory in MB to be available
      mem_per_cpu:
        type: integer
        description: memory in MB used per cpu
      partition:
        type: string
        description: partition to use on cluster
      threads:
        type: integer
        description: number of threads to be available
      time:
        type: string
        description: max execution time

  smn_charts:
    description: resource definitions for smn_charts
    type: object
    properties:
      mem_mb:
        type: integer
        description: max memory in MB to be available
      mem_per_cpu:
        type: integer
        description: memory in MB used per cpu
      partition:
        type: string
        description: partition to use on cluster
      threads:
        type: integer
        description: number of threads to be available
      time:
        type: string
        description: max execution time

  svdb_merge:
    description: resource definitions for svdb_merge
    type: object
    properties:
      mem_mb:
        type: integer
        description: max memory in MB to be available
      mem_per_cpu:
        type: integer
        description: memory in MB used per cpu
      partition:
        type: string
        description: partition to use on cluster
      threads:
        type: integer
        description: number of threads to be available
      time:
        type: string
        description: max execution time

  svdb_query:
    description: resource definitions for svdb_query
    type: object
    properties:
      mem_mb:
        type: integer
        description: max memory in MB to be available
      mem_per_cpu:
        type: integer
        description: memory in MB used per cpu
      partition:
        type: string
        description: partition to use on cluster
      threads:
        type: integer
        description: number of threads to be available
      time:
        type: string
        description: max execution time

  tiddit:
    description: resource definitions for tiddit
    type: object
    properties:
      mem_mb:
        type: integer
        description: max memory in MB to be available
      mem_per_cpu:
        type: integer
        description: memory in MB used per cpu
      partition:
        type: string
        description: partition to use on cluster
      threads:
        type: integer
        description: number of threads to be available
      time:
        type: string
        description: max execution time

  required:
    - default_resources
